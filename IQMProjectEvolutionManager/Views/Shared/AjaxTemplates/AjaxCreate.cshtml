@model EstatesManagement.Core.Interfaces.IAjaxViewModel
@using (Ajax.BeginForm("AjaxCreate", Model.GetDomainObjectName(),
    new { timestamp = DateTime.Now.Ticks },
    new AjaxOptions
    {
        UpdateTargetId = "partialViewDialogData",
        InsertionMode = InsertionMode.Replace,
        OnBegin = "ESTMAN.AjaxUtils.BeginRequest",
        OnComplete = "ESTMAN.AjaxUtils.EndRequest('" + @Model.GetDomainObjectName() + "', '" + @ViewData["returnObjId"] + "')",
        OnFailure = "Global.AjaxFormError"
    },
    new { name = "Create" + @Model.GetDomainObjectName() + "Form", id = "Create" + @Model.GetDomainObjectName() + "Form" }
    ))
{
    <div>
        @Html.ValidationSummary(false)
    </div>
    @Html.EditorFor(model => model)
    <table>
        <tr>
            <td>
                <button type="button" class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only"
                        role="button" aria-disabled="false" rel="Cancel">
                    <span class="ui-button-text">Cancel</span>
                </button>
                <button type="submit" class="ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only"
                        role="button" aria-disabled="false" rel="Save">
                    <span class="ui-button-text">Save</span>
                </button>
            </td>
        </tr>
    </table>
}